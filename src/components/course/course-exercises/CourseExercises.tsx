import React from "react";
import * as MUI from '@mui/material';

function CourseExercises()
{
    return (
        <MUI.Box>
            CourseExercises Works!
        </MUI.Box>
    );
}

export default React.memo(CourseExercises);