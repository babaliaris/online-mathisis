import React from "react";
import * as MUI from '@mui/material';

function CourseComments()
{
    return (
        <MUI.Box>
            CourseComments Works!
        </MUI.Box>
    );
}

export default React.memo(CourseComments);